from setuptools import setup

setup(name='pontomais-functions',
      version='1.0',
      description='Functions package to interact with Pontomais APIs',
      packages=['functions'],
      zip_safe=False)